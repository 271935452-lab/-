# 船务组需求 — 待产品确认清单

> **依据**：20260826 改版后的 11 份分册 PRD + 2 份总 PRD + 11 份原型 + 1 份邻接原型（关务组清关管理）+ **现有 `tx-parent` 代码库实扫**
> **整理时间**：2026-08-25
> **用途**：本文档每一条都需要产品/业务给一个明确答复，**答复前对应功能不开工**
> **怎么用**：直接在「产品答复」列填字，或圈掉不要的选项。带 🔴 的是阻塞项，不定就没法动手。

---

## 怎么读这份文档

问题按**为什么需要确认**分了六类，不是按功能模块分的：

| 类 | 含义 | 条数 |
|---|---|---|
| **P** | 🔴 阻塞开工 —— 不定这几条，整块功能没法开始 | 4 |
| **D** | 文档本身的缺陷 —— 文档不改，开发没有验收依据 | 5 |
| **X** | 口径矛盾 —— 两份文件写的不一样，必须选一个 | 6 |
| **N** | PRD 未覆盖 —— 我从原型代码反推发现的，业务方可能还不知道 | 13（N5 已撤回） |
| **W** | PRD 自己标了「待定」的 | 9 |
| **C** | **扫现有代码后发现的 —— PRD 与已实现代码不一致** | **3** |

**合计 40 条。** 其中 **P 类 4 条 + C1 请优先答复**。

> **✅ N5 已撤回**：原判「查验率算不出来」是我只看原型时的误判，现有代码早已用时间戳正确实现。详见 N5 那一节。

---

## 零、P 类 · 阻塞开工（🔴 请优先答复）

### P1 🔴 权威口径以哪份文件为准？

**现状**：本轮改版后出现了**三份**都叫「总 PRD」的东西，内容互相冲突：

| 文件 | 大小 | 我理解的定位 |
|---|---|---|
| `船务组-需求PRD.html` | 21.7 KB | **索引总 PRD** —— 只有 背景/约定/页面对照/跨页联动17条/配置数8项/二期待定，正文都下沉到分册了 |
| `船务组-需求PRD-产品部全文-20260826.html` | 164 KB | 产品部全文 —— 但里面的「跨页联动表」是**旧版**，与索引总 PRD 有 4 处直接矛盾 |
| 11 份分册 PRD | 4~45 KB | 各页的详细口径，测试点都在这里 |

**我的判断（需要你确认）**：
- 分册 PRD = **最高权威**（测试点在这里，验收看这里）
- 索引总 PRD = **权威**，但只管跨页联动和全局配置数
- 164KB 产品部全文 = **非权威、不作验收依据**，只当背景资料读

**不确认的后果**：开发照 164KB 那份做，会做出「飞驼进港自动抓取」等已经砍掉的功能（见 X1）。

**产品答复**：______________________

---

### P2 🔴 运价管理是不是真的整块不做了？

**证据**：
- 本轮**没有**运价管理的分册 PRD
- 原型 `船务组-运价管理-我司与代理订舱-MVP.html`（56.9 KB）**没有搬到新版目录**
- 索引总 PRD 的「页面对照表」里**没有这一页**

**连带影响**（如果确认不做，这些全部作废）：
- 「合约价确认开关」功能（本轮 11 份分册 PRD 里**零命中**）
- 端到端链路的步骤 0 / 0b
- 九项日常的 **#0 运价维护与核对**（陈秋玉的岗位职责本期没有系统落点）
- 运价价目表建表（19 个字段）
- 「同步报价组」推送运价（注意：推送**客户协议价**是另一回事，那个还做）

**上一版地图把它列为模块 1 + 链路第一步 + 新增岗位职责**，如果确实不做，这是本期最大的一块范围缩减。

**产品答复**：
- [ ] 确认整块不做，本期运价仍走线下 Excel + 邮件
- [ ] 要做，PRD 漏发了，请补
- [ ] 其他：______________________

---

### P3 🔴 提单号规则（船司前缀字典）确认二期？

**证据**：明确标注「**二期**」。但原型 `数据字典-船公司航运-提单号规则-MVP.html`（23.3 KB）+ PRD（3 KB）都存在。

**麻烦在于**：提单创建 AI 识别里有一条联动是「**船公司 → 提单号前缀（替换非叠加）**」，这条联动**依赖**前缀字典。

**所以要么**：
- A. 前缀字典二期做 → **AI 识别的这条联动本期也去掉**
- B. 前缀字典的**数据部分**本期先建表建维护页，只是不做复杂的规则校验 → AI 联动可以做
- C. 前缀先硬编码在代码里，二期再做成可维护字典

**产品答复**：选 ______，理由：______________________

---

### P4 🔴 服装货的默认品名词表在哪里？

**背景**：终配舱自动配舱的五档里，第 2 档「服装货」不靠人工勾选，靠**品名包含匹配**自动判定。

**问题**：那份 40 多个词条的默认词表（服装/衣服/男装/女装/T恤/卫衣/clothing/apparel… + 排除词 衣架/人台/模具…），**在本轮 11 份分册 PRD 和 11 份原型里都找不到原文**。它只存在于**上一轮的旧总 PRD（104 KB 版本）**里。

**后果**：这一档现在**没有现行文档依据**，开发要么硬编码一份来源不明的词表，要么这档做不了。

**产品答复**：
- [ ] 把词表补进「终配舱-MVP-PRD」，我按补的做
- [ ] 沿用旧总 PRD 那份，我照抄（**但请确认词表内容不变**）
- [ ] 词表做成后台可配置，初始数据由业务方填，我只做维护页
- [ ] 其他：______________________

---

## 一、D 类 · 文档本身的缺陷

这几条不是业务问题，是**文档写漏或写错了**，改文档就行，但不改开发没有验收依据。

| # | 问题 | 证据 | 建议 | 产品答复 |
|---|---|---|---|---|
| **D1** | **索引总 PRD 的「页面对照表」漏了「销售产品·报价管理·批量调价」这一页** | 该页有独立分册 PRD（4.4 KB）+ 原型（22.7 KB），但页面对照表里查不到 | 补进对照表 | |
| **D2** | **配置数表里「配舱档」的指向写错了** | 索引总 PRD 的配置数 8 项里，「配舱档」这一项指向的位置与实际口径所在的分册不一致 | 改指向「终配舱-MVP-PRD」 | |
| **D3** | **关务组「清关管理-MVP」原型没有配套 PRD** | 原型 56.3 KB 存在，同名 PRD 请求返回 404 | 该页归关务组，但它**只读消费船务落库的进口商×清关行**（见 N9），至少要有一份接口约定 | |
| **D4** | **操作日志「不记」清单里的「企微复制」是上一轮残留** | 索引总 PRD 已明确提单分配**不对接企微群**（本期只有一键复制到剪贴板），既然没有企微推送，「企微复制不写日志」这条就无意义 | 改成「**复制到剪贴板** 不写日志」。实现上无差别，但文档口径要清，否则读的人会以为有企微对接 | |
| **D5** | **`_new` 目录里有失效链接** | 新版归档目录内部分页面互链指向了旧版文件名，点开 404 | 发新版时一并修 | |

---

## 二、X 类 · 口径矛盾（两份文件写的不一样，必须选一个）

### X1 🔴 飞驼进港自动抓取 —— 本期做还是不做？

| 文件 | 写的是 |
|---|---|
| 索引总 PRD 跨页联动表 | **本期不做** |
| 164 KB 产品部全文 | **本期做** |

**牵连的功能**（如果不做，这些全砍）：
- 开船后自动拉数写入进港时间
- 「飞驼轨迹自动抓取状态」列的三态（绿底"自动" / 橙标"待确认" / 空白）
- 进港代码的「系统建议码 vs 抓取值」比对三态

**注意一个连锁风险**：落箱费（F1）的触发时机**依赖进港时间**。如果自动抓取不做，等于「落箱费能不能出」100% 靠人工填进港时间 —— **没人填，落箱费就永远不出**。这是本期最脆的一环。

**我倾向**：以索引总 PRD 为准（不做），但需要产品确认，并且要想清楚 F1 的人工兜底怎么保障。

**产品答复**：______________________

---

### X2 取消 IT 的三个值 vs 两个值

| 侧 | 枚举 |
|---|---|
| **船务侧**（提单管理，§3.8） | 有IT / 已取消IT —— **两值**，且「已取消不可改回有IT」 |
| **关务侧**（清关管理，只读列） | 未取消 / 已取消 / **取消中** —— **三值** |

**「取消中」这个中间态是谁写的？** 完全没有定义。

如果是「船务点了『取消IT标记』后异步等关务回执才落终态」，那：
- 船务侧数据库要存**三值**，不是两值
- 操作日志要记**两次**（发起 + 回执）
- 还要定义回执失败/超时怎么办

**产品答复**：
- [ ] 「取消中」是关务侧自己的展示态，船务侧不用管，仍存两值
- [ ] 船务侧也要存三值，异步流程如下：______________________
- [ ] 其他：______________________

---

### X3 舱位播报的船期能不能手改？

| 文件 | 写的是 |
|---|---|
| 0820 修正稿 | **必须保留手改权限**，可改预计起航与截单 |
| 20260826 分册 | 飞驼船期**只读** |

**业务上手改是有必要的** —— 飞驼截单时间常滞后，播报发出去客服要按**实际截单**收货。

**产品答复**：
- [ ] 只读，不给改
- [ ] 可改，但要留痕（记谁改的、改前改后）
- [ ] 可改，不留痕
- [ ] 其他：______________________

---

### X4 海放 / 码放的细分，关务侧要不要同步？

**船务侧**把放行拆得很细：海放 = 轨迹 `TR00803`，码放 = 轨迹 `A00178`，筛选有 **8 个组合值**。

**关务侧**（清关管理页）只有一个粗状态 `清关放行`，**没有这层细分**。

**问题**：这层细分是船务独有的，还是关务侧也应该有？如果关务侧要看，得同步。

**产品答复**：______________________

---

### X5 「待终配舱拆两态」这条约束挂到哪个模块验收？

**背景**：现网有个合并状态「待终配舱」，上一版 PRD **明令禁止再写合并态**，要求拆成「待配舱」和「终配舱」两个独立状态。

**问题**：这条约束原来写在「运单管理·签入代办」模块下，但那个模块本轮**原型和 PRD 都没有搬迁，导航也不再链接它**。

**这条约束不能跟着页面一起丢** —— 终配舱独立成模块之后，状态拆分**更需要成立**。

**产品答复**：
- [ ] 挂到「终配舱-MVP」模块下验收
- [ ] 挂到「待配仓-MVP」模块下验收
- [ ] 签入代办模块本期还是要做，PRD 漏发了
- [ ] 其他：______________________

---

### X6 「运单管理·签入代办」整个模块本期做不做？

**证据**：旧版有原型 `运单管理-签入代办角标弹窗-MVP.html`（50.4 KB），**新版目录里没有对应文件**，船务组导航页不再链接它，索引总 PRD 页面对照表也没有这一页。

**牵连的功能**：
- 签入代办触发（`已签入 && 报关资料未上传` → 给业务员和客服各生成一条）
- 角标 5 处显示 + 实时推送
- 预录确认三态（未出预录单 disabled / 已出待确认可反悔 / 已上传放行单永久锁定）

**产品答复**：
- [ ] 确认本期不做
- [ ] 要做，PRD 漏发了
- [ ] 其他：______________________

---

## 三、N 类 · PRD 未覆盖，从原型代码反推发现的

> 这一类的共同特点：**PRD 没写，但原型代码里能看出来有问题**。业务方可能还不知道。
> 风险比 W 类高 —— W 类是「已知的未知」，N 类是「未知的未知」。
> 每条都标了**我的判断依据**，凡是我推断的都写明了「反推」。

### N1 待配仓的分组勾选有 bug（已确认）

**现象**：待配仓列表按运单分组显示，**分组头的勾选框和组内行的勾选框状态不联动**。勾了组头，组内行没被选中；反选组内某行，组头不变成半选态。

**依据**：原型 JS 里勾选状态存的是行级 id 集合，分组头的 checkbox 是**独立的 DOM 状态**，没有双向同步逻辑。

**影响**：用户以为勾了一整组，实际只提交了组头那一行 → **批量落预配舱号会漏单**。

**需要确认**：分组勾选的正确口径是什么？
- [ ] 勾组头 = 勾组内全部行（组内行变化时组头显示 全选/半选/未选 三态）
- [ ] 组头勾选框去掉，只保留行级勾选
- [ ] 其他：______________________

---

### N2 待配仓完全没有容量校验（反推）

**现象**：分册 PRD 里有柜容积 / 限重约束的描述，但**原型里完全没有校验代码** —— 勾到超柜也不拦、不提示。

**依据**：搜遍待配仓原型，没有任何体积/重量累加与阈值比较的逻辑；也没有柜型容量的配置数据。

**需要确认**：
- [ ] 本期就是不做容量校验，人工把控（那 PRD 里的容积/限重描述请删掉，避免开发误做）
- [ ] 要做，请给出：柜型 → 容积/限重 的对应表，以及超限是硬拦还是软提示
- [ ] 其他：______________________

---

### N3 清关行的「不接港口」配了，但绑柜时从不比对（反推）

**现象**：清关行港口配置页可以给每家清关行维护「不接港口」黑名单，矩阵也做了三态循环（未分配/授权/禁接）。**但柜子绑定进口商+清关行时，完全没有比对目的港或中转港。**

**依据**：绑定弹窗的 JS 里只查了授权态（禁接的不出现在下拉里），**没有任何港口比对代码**。

**为什么这条风险高**：关务组的清关管理页是**只读消费**船务落的清关行，**不校验不兜底**（见 N9）。所以把柜派给了不接该港的清关行，**关务侧无从发现**，直接体现为发错邮件、催错放行。

**这是 H7 链路上唯一可能的护栏，如果不做，这条写入链路就完全没有校验。**

**需要确认**：
- [ ] 本期补上比对：绑定时若清关行的不接港口命中该柜目的港/中转港 → 硬拦 / 软提示（选一个：______）
- [ ] 本期不做，二期补（**但请知晓上述风险**）
- [ ] 其他：______________________

---

### N4 已绑定的柜子，清关行授权被撤销后不会回滚（反推）

**现象**：进口商×清关行的授权态可以从「授权」改成「禁接」。但**已经绑好的柜子不会自动回滚，也没有任何提醒**。

**依据**：授权矩阵的变更操作只写授权关系表，没有反查已绑柜子的逻辑。

**需要确认**：授权被撤销时，存量已绑柜子怎么处理？
- [ ] 不管，历史绑定保留（那需要一个存量校验报表，让人工发现）
- [ ] 给出提醒/待办，人工去改
- [ ] 自动解绑并要求重选
- [ ] 其他：______________________

---

### ~~N5 查验率的公式没法按当前状态实现~~ ✅ **已撤回，不用答复**

> **2026-08-25 扫代码后撤回。** 这条是我只看原型时的误判 —— **现有代码早就按时间戳正确实现了。**

`tx-oms/.../importer/service/impl/ImporterUsageSummaryServiceImpl.java`：

| 我原先担心的 | 代码实际做法 |
|---|---|
| 三态互斥算不出分母 | `isCheckRateBase()` = `清关放行` ∪ `查验放行` ∪ `清关查验` |
| 分子取不到「曾被查验」 | `isInspectedStatus()` = `清关查验` ∪ `查验放行` |
| 没有时间戳 | 用 `CUSTOMS_VERIFY_START_TIME` / `CUSTOMS_VERIFY_PASS_TIME` / `CUSTOMS_PASS_TIME`（都在 `S_SCAN_FINAL_STOWAGE_CUSTOMS`），取不到才兜底绑柜时间 |
| ≥10% 阈值 | `ABNORMAL_RATE = BigDecimal.TEN`，7 天 / 本月 / 累计**任一**超标即标异常 |

**上面举的「0% vs 20%」那个例子，现有实现算出来是正确的 20%。**

**但扫代码时发现了三条新的、真的要问的**，见下面 C1 / C2 / C3。

---

### 🔴 C1 一水绑柜的「5」，现有代码是 5 **天**，PRD 是 5 **柜**（扫代码发现）

`S_IMPORTER_USAGE_SUMMARY` 有个现成字段 `IN_TRANSIT_OVER_FIVE_CNT`，代码逻辑是：

```java
if (source.getBindTime() != null && source.getBindTime().before(fiveDaysAgo)) {
    inTransitOverFiveCnt++;   // 在途未清关，且绑柜时间超过 5 天
}
```

**现有代码**：在途未清关 且 绑柜已超过 **5 天** → 计数。这是「压柜超期」。
**PRD 9.6**：同 (进口商, 船, 预计装柜日) 绑柜数 ≤ **5 柜**。这是「一水超限」。

**同一个字段名，两个完全不同的业务含义。**

**需要确认**：
- [ ] PRD 的「≤5 柜」是**新口径，替换**掉原来的「超 5 天」
- [ ] 两个指标**都要**，各占一列（那需要给第二个字段命名）
- [ ] 原来的「超 5 天」才是对的，PRD 写错了
- [ ] 其他：______________________

> ⚠️ 这条不定清楚，要么数据算错，要么白做一遍。**建议优先答复。**

---

### C2 清关状态实际有 5 个，PRD 只写了 4 个（扫代码发现）

代码里的清关状态常量（`ImporterUsageSummaryServiceImpl`）：

`资料准备` / `待清关` / `清关查验` / `清关放行` / `查验放行`

**PRD 只写了后 4 个，漏了「资料准备」。**

**需要确认**：
- [ ] 「资料准备」保留，PRD 补上
- [ ] 「资料准备」要废弃，合并到「待清关」
- [ ] 其他：______________________

---

### C3 存量终配舱号怎么办（扫代码发现）

现有 `S_SCAN_FINAL_STOWAGE.FINAL_STOWAGE_NO` 已有大量存量数据，格式**不是** PRD 要求的 `{YYMMDD}-{销售产品名称}-{序号}`。

**需要确认**：
- [ ] 新规则**只对新单生效**，存量单号不动（新旧格式共存）
- [ ] 存量单号**批量重新生成**（需评估对下游报关、PDA、财务的影响）
- [ ] 其他：______________________

---

### N6 协议价的「全量重填」是死代码，但删得不干净（反推）

**现象**：协议价维护页的「全量重填」模式，**三个入口都删了**，但底层代码还在：
`MODE_LABEL.full`、`modeRuleText` 里的「全量重填」文案、`calcFinal` 的 `full` 分支、`isFullStructSlim()`、`hit === "回退原价"` 判定、以及 `flatPriceBar` 整块 UI（统一价格 + 生效时间）。

**依据**：Grep 原型 JS，入口 DOM 已删但函数体和常量都保留。

**需要确认**：
- [ ] 确认「全量重填」和「大客户调成本」本期都不做，这些是残留代码，开发不用管
- [ ] 「全量重填」还是要的，入口误删了
- [ ] 其他：______________________

---

### N7 协议价的 `overrides` 键是字符串拼接（反推，影响建表）

**现象**：原型里 `overrides` 的 key 形如 `{报价id}::{重量段}|{分区}`，是字符串拼出来的。

**影响**：**建表必须拆成子表**（报价id / 重量段 / 分区 三个独立列 + 覆盖价），不能照搬拼接键存成一个 varchar，否则没法按分区或重量段查询和统计。

**这条不需要产品答复，是给开发的提醒。** 但如果产品有「按分区导出」「按重量段统计」之类的后续需求，请现在说，我一次建对。

**需要确认**：后续有没有按分区/重量段维度查询或统计的需求？______________________

---

### N8 协议价的期间互斥有几个漏洞（反推）

**新增的「发布协议价表 + 期间互斥」是本轮最核心的改动**（替代了原来的优先级机制）。但原型的互斥校验有漏洞，以下场景没拦住：

| 场景 | 现状 | 应该 |
|---|---|---|
| 新增的期间**完全包含**已有期间 | 不拦 | 应拦 |
| 新增的期间**被已有期间完全包含** | 不拦 | 应拦 |
| 期间**端点相接**（前一条止 = 后一条起） | 不拦 | 需明确：算重叠还是算连续？ |
| **开放式期间**（无止期）与后续新增 | 不拦 | 应拦 |

**需要确认**：
1. 端点相接（比如 8/1–8/15 和 8/15–8/31）算重叠吗？ [ ] 算 [ ] 不算，8/15 归后一条 [ ] 不允许出现这种录入
2. 允许「无止期」的开放式协议价吗？ [ ] 允许（那新增时要自动截断前一条） [ ] 不允许，必须填止期
3. 互斥的判定维度是什么？（客户 + 渠道？还是 客户 + 渠道 + 目的地？）______________________

---

### N9 关务侧只读消费船务落的清关行，不校验不兜底（已确认，但责任边界要产品认）

**现状**：关务组新增的清关管理页，把「进口商 / 清关行」当成**只读带出列** —— **关务侧不校验、不兜底、不允许改**。

**意味着**：船务在柜子绑定进口商那一刻选定的清关行，就是关务后续发邮件、要 AN/DO、催放行的**唯一依据**。船务落错，关务无从发现。

**需要确认**：
- [ ] 确认这个责任边界（船务 100% 负责清关行落库正确性），并同意补 N3 的港口比对校验
- [ ] 关务侧也要做一层校验兜底
- [ ] 其他：______________________

---

### N10 加载加费本期被强制挂在提单上（口径变化，需确认）

**旧口径**：加载加费（CW021）**必须挂柜**，可另挂票。
**新口径**：本期强制挂**提单**。

**问题**：一个提单可能对应多个柜。加载加费本来是「某个柜重装」产生的，挂到提单上之后**丢失了「是哪个柜」的信息**。

**另外**：多柜场景下费用按提单**去重不均摊** —— 意思是同一提单下多个柜的加载加费只记一次？还是记多次但不分摊到柜？

**需要确认**：
1. 加载加费挂提单后，还需要记录「是哪个柜触发的」吗？ [ ] 需要，加个柜号字段 [ ] 不需要
2. 「多柜按提单去重不均摊」的准确含义是：______________________

---

### N11 费用登记的三条挂载路径，优先级和冲突怎么处理（反推）

**现状**：费用可以挂**提单** / 挂**柜** / 挂**票**，三条路径。

**问题**：如果同一笔费用被从两个入口各录了一次（比如既挂了提单又挂了柜），系统怎么处理？现在没有去重或提示。

**需要确认**：
- [ ] 三条路径互斥，一笔费用只能选一个挂载维度
- [ ] 可以重复挂，但要提示可能重复
- [ ] 其他：______________________

---

### N12 四个弹窗有完整实现但没有触发入口（已确认是刻意的）

**现象**：查验标记 / 查验挂票 / 改单标记 / 进港代码，这 4 个弹窗在原型里**有完整实现，但工具栏上没有对应按钮**。

**已确认的解释**：它们是本期**刻意去掉的入口**，代码留着但不接按钮。

**这条不需要答复，写在这里是为了防止开发「顺手补上」。** 但请确认一下：

- [ ] 确认这 4 个入口本期都不做
- [ ] 其中 ______ 是要做的，工具栏漏画了

---

### N13 「合并报关拆柜」是新增的硬约束，但拆柜的判定标准没写（反推）

**现状**：待配仓新增了一个「合并报关·配仓提醒」弹窗，提示「配仓请尽量同柜」。这是待配仓本轮**唯一新增的业务约束**。

**问题**：「无故拆柜」的判定标准是什么？多少算「无故」？

比如：3 票合并报关，前 2 票装满一柜，第 3 票只能开新柜 —— 这是**容量所迫**，不是无故。系统怎么区分「容量所迫」和「无故拆分」？

**在 N2（没有容量校验）的前提下，系统根本不知道柜满了没有，所以无法区分。**

**需要确认**：
- [ ] 只做提示，不做智能判定 —— 只要检测到同一合并报关组被分到 ≥2 个柜就提示，人工判断
- [ ] 要做智能判定，请先解决 N2（给出柜容量数据）
- [ ] 其他：______________________

---

### N14 原型里的硬编码常量，哪些真的要做成可配置

**原型里写死的**：
| 常量 | 值 | 含义 |
|---|---|---|
| `NEAR_CUT_HOURS` | 24 | 临近截单阈值（小时） |
| `NEAR_SAIL_DAYS` | 3 | 临近未海放/码放窗口（天） |
| `IMP_LIMIT` | 5 | 一水绑柜上限 |
| `NOW` | 2026-08-07 10:00 | 原型演示用的基准时刻（这个当然不用管） |

**PRD 说「全部可配置」**，但可配置有三个档次，成本差很多：

| 档 | 做法 | 工时 |
|---|---|---|
| A | 写在配置文件 / 常量类，改了要发版 | 最省 |
| B | 存字典表，后台字典页面可改 | 中 |
| C | 做专门的「参数配置」页面，带权限和变更记录 | 最贵 |

**需要确认**：这几个阈值要哪一档？
- [ ] A —— 基本不会改
- [ ] B —— 偶尔要改，走字典
- [ ] C —— 业务要自己随时调，要专门页面
- [ ] 分开：______________________

---

## 四、W 类 · PRD 自己标了「待定」的

> 这一类业务方已经知道没定，我只是把它们集中列出来催一下。
> **凡是标「待定」的，都不能作为开发验收口径** —— 开发照待定项做，验收时一定对不上。

| # | 问题 | PRD 里的备选 / 现状 | 影响什么 | 产品答复 |
|---|---|---|---|---|
| **W1** | **删除或作废提单后，费用要不要清掉**（CW016） | A 删除时清费用 / B 只删提单、费用保留 / C 有费用则必须先清费用再删提单。**PRD 自己注明「留费用会导致分摊错误」** | 费用登记模块。作废入口已从工具栏去掉，所以本期只需要定「删除」的情况 | |
| **W2** | **飞驼覆盖港口清单由谁维护** | 未定 | 直接影响落箱费 F1 能否自动出。**在 X1 确认「自动抓取不做」的前提下，这条优先级降低，但没消失** —— F1 触发仍依赖进港时间 | |
| **W3** | **附加费的单位到底是什么** | 产品库表内写的 0.5 / 3，是 USD/kg？还是按票/按柜？**原型暂按 USD/kg 实现** | 产品库·收费标准。单位错了金额全错 | |
| **W4** | **「有条件出」不满足条件时怎么办** | 系统硬拦 vs 仅预警 | 产品库·出货要求 | |
| **W5** | **反倾销列表的数据结构** | 源表暂空，原型只留了入口 | 终配舱的反倾销硬约束**依赖这个数据**。数据结构不定，反倾销集中装柜做不了 | |
| **W6** | **分配提单的规则维度与推荐策略** | 0730 遗留争议，未定 | 提单分配。**如果本期只做手工指派拖车，这条可以不定**；如果要智能推荐，必须定 | |
| **W7** | **各节点标准费用项清单与供应商维度** | 0730 遗留争议，未定 | **注意：0730 的九费用节点方案已整体作废**，本期只做「费用登记」。所以这条**可能已经不需要定了**，请确认 | |
| **W8** | **「取消IT·中转港」与普通取消IT的差异** | 0730 遗留争议，未定 | 取消IT 功能。与 X2 的「取消中」是两个不同的问题，别混 | |
| **W9** | **反倾销等标记的枚举、维护方、可见方** | 0730 遗留争议，未定 | 影响 终配舱（反倾销约束）+ 提单管理（业务标记展示）。**反倾销标记来源现在是「申报带出」和「船务手标」两者都有**，那维护方就是两方，需要明确各自权限 | |

---

## 五、已作废，不用再问（备查）

上一版清单里的这几条，本轮已经有答案了，**不需要产品再答复**，列出来是为了避免重复沟通：

| 原编号 | 问题 | 现在的状态 |
|---|---|---|
| A2 | 合约价确认开关的配置菜单位置 | **失效** —— 运价管理本期不做（P2），开关本身已作废 |
| A4 | 船司运价是否先维护 | **失效** —— 运价管理整块移出本期 |
| A5 | 智能配柜的范围 | **已答复** —— 终配舱分册 PRD 已给出边界：五档装填 + 反倾销硬约束 + 服装品名匹配，**不含容量最优化求解** |
| — | 「配仓」还是「配舱」 | **已定案** —— 页面名和口语用「配**仓**」（菜单「待配仓」、弹窗「配仓提醒」）；**所有字段名/状态/单号/模块名用「配舱」**（`预配舱单号`、`终配舱单号`、`配舱状态`、`自动配舱`）。建库列名一律走「舱」 |
| — | 应收三列不闭合 / 回款率快照飘移 | **已定案保留现状**，后端不动 |

---

## 六、优先级建议

如果没法一次全定，建议按这个顺序：

**第一批（本周内，否则开不了工）**
- P1 权威口径以哪份为准
- P2 运价管理是否整块不做
- P3 提单号规则是否二期
- P4 服装货词表在哪
- X1 飞驼进港自动抓取做不做
- **C1 一水绑柜的「5」是 5 天还是 5 柜**（现有代码与 PRD 同名不同义，不定就会算错数）

**第二批（开发前必须定，影响建表）**
- ~~N5 查验率公式~~ ✅ **已撤回，代码已正确实现**
- **C3 存量终配舱号是否沿用旧格式**（影响 `S_SCAN_FINAL_STOWAGE` 存量数据处理）
- N8 协议价期间互斥的三个细节（影响核心表设计）
- X2 取消IT 三值还是两值（影响字段类型）
- N10 加载加费挂提单要不要留柜号
- W5 反倾销数据结构

**第三批（开发中可以边做边定）**
- N1 分组勾选口径
- N2 容量校验做不做
- N3 港口比对做不做
- N13 合并报关拆柜判定
- N14 阈值可配置到哪一档
- X3 播报船期能不能手改
- **C2 清关状态第 5 态「资料准备」保留还是废弃**

**第四批（不阻塞，但要收口）**
- D1~D5 文档缺陷
- X4 / X5 / X6
- N4 / N6 / N7 / N9 / N11 / N12
- W1~W9（其中 **W6 拖车推荐已降级** —— `S_PRICE_TRUCK_HEAD` 报价数据已存在，只需定推荐策略）

---

## 附录：本文档的证据来源

| 类型 | 数量 | 说明 |
|---|---|---|
| 索引总 PRD | 1 | `船务组-需求PRD.html` 21.7 KB |
| 产品部全文 | 1 | `船务组-需求PRD-产品部全文-20260826.html` 164 KB（**非权威**） |
| 分册 PRD | 11 | 提单管理 44.6 KB / 提单创建AI 19.9 KB / 客户协议价 17.3 KB / 产品库出货 15.5 KB / 终配舱 13.2 KB / 待配仓 10.8 KB / 进口商管理 10.4 KB / FBA合规 9.1 KB / 清关行港口配置 5.9 KB / 柜子编辑 5.8 KB / 批量调价 4.4 KB |
| 功能原型 | 11 | 与分册 PRD 一一对应 |
| 邻接原型 | 1 | 关务组 `清关管理-MVP.html` 56.3 KB（**无 PRD**） |
| 旧版留档 | 8 | 用于对比找出本轮变化，含旧总 PRD 104 KB、运价管理原型、提单号规则原型、签入代办原型等 |

**归档位置**：`docs/temp/shipping-proto/`（新版在 `_new/`，旧版在根目录，含带 SHA 比对的重扫脚本 `_rescan.py`）

**完整的功能地图**（含全部规则阈值、数据模型线索、角色权限）：`docs/船务组功能地图/船务组功能地图.html`

---

> **关于「反推」二字**：本文档 N 类里凡标「反推」的，都是我从原型 JS 代码和 DOM 结构推断出来的，**不是 PRD 原文，也不是代码确证**。可能是原型做得简略而非真的设计遗漏。请业务方核对后再定。


