# 船务组需求 — 功能点清单与现有代码对比

> **用途**：工时评估。每个功能点标明**是全新建还是改造现有**，以及依赖哪些前置。
> **依据**：20260826 改版后的 11 份分册 PRD + 11 份原型 + 现有 `tx-parent` 代码库扫描
> **整理时间**：2026-08-25

---

## 怎么读这份文档

### 类型标记

| 标记 | 含义 | 工时特征 |
|---|---|---|
| 🆕 **全新** | 表、代码都没有，从零建 | 最贵 |
| 🔧 **改造** | 有现成的表/代码，扩字段或加逻辑 | 中 |
| ♻️ **复用** | 现有能力直接调，只需接线 | 便宜 |
| ✅ **已完成** | **代码里已经实现了，不用做** | 0 |
| ⚠️ **阻塞** | 有未确认的前置问题，**不能开工** | 无法估 |
| ⛔ **不做** | 本期明确移出范围，**不要算进工时** | 0 |

### 复杂度分档

| 档 | 含义 | 参考人日（后端，含单测） |
|---|---|---|
| **S** | 单表 CRUD / 加字段 / 加筛选条件 | 0.5 ~ 1 |
| **M** | 多表联查 / 有业务校验 / 有状态流转 | 2 ~ 4 |
| **L** | 有算法或复杂规则 / 跨模块联动 / 需要设计表结构 | 5 ~ 8 |
| **XL** | 核心算法 / 涉及历史数据迁移 / 跨组接口协商 | 10+ |

> 人日是**初估**，用于排期粗排，不是承诺。前端工时另计（原型已画的页面前端相对确定）。

### 一个重要前提

带 ⚠️ 的功能点，其未确认问题都编号对应到 **`待产品确认清单.md`**。**那份文档里的 P 类 4 条不定，本期开不了工。**

---

## 零、本期范围一句话

**做**：提单管理（列表+创建+分配+换柜换船+费用登记+日志）· 待配仓 · **终配舱（新独立模块）** · 进口商管理 · 清关行港口配置 · 柜子绑定进口商 · 客户协议价维护 · 产品库出货要求 · 报价管理批量调价 · FBA 录入合规策略 · 舱位播报

**不做**（上一版算进去了，本轮砍掉，**工时要扣**）：运价管理 · 合约价确认开关 · 提单号规则字典 · 飞驼进港自动抓取 · 舱位播报推企微群 · 九费用节点方案 · 协议价全量重填 · 运单管理签入代办

---

## 一、逐模块功能点分解

### 1 · 提单管理列表

**页面**：提单管理主列表（43 列）+ 底栏周页签 + 预警胶囊条 + 工具栏

> 🔧 **主表已存在** = `S_BUSS_SAILING_POSITION`（50 字段，提单号/柜号/铅封/船名航次/截单/免箱天数/换船标记/进港代码都有）。周分组和装完预警的数据源在 `S_SCAN_FINAL_STOWAGE`。详见 4.2 / 4.3。

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 1.1 | 列表分页查询（43 列） | 🔧 | M | 主表字段大半已有，主要是扩列 + 联查 |
| 1.2 | 提单状态页签（待分配/待装柜/待离港/已离港/全部） | 🔧 | S | 注意「待装柜」原名「待录卸」，需改枚举 |
| 1.3 | **按周分组底栏页签** | 🔧 | M | **依据改了**：从 ETD 所在周 → **预计装柜时间所在周**（ISO 周一起算）。✅ 数据源 `EXPECTED_LOADING_TIME` 已有，只需实现 ISO 周反算 |
| 1.4 | 周角标计数（不分页统计该周总条数） | 🆕 | S | 与 1.3 同一个查询，加 count |
| 1.5 | 分页作用域限定在当前周 + 序号连续 | 🆕 | S | 每页 10 时第 2 页从 11 起 |
| 1.6 | 5 种操作回第 1 页 | 🆕 | S | 切周/切状态/点预警/查询/改条数 |
| 1.7 | **预警胶囊条**（5 类计数 + 点击筛选） | 🆕 | L | 见 1.8~1.12，每类一套判定 |
| 1.8 | 预警：临近截单（距截单 ≤24h 且未到截单） | 🔧 | S | ✅ `CY_CUTOFF` / `PORT_CUTOFF` 已有。阈值可配置档次待定（⚠️ N14） |
| 1.9 | 预警：临近未海放 / 未码放 | 🆕 | M | **基准改了**：ETD → **预计出发时间**，窗口 今日~今日+3 天。**海放/码放标记全库无实现，要加列** |
| 1.10 | 预警：未进港 | 🔧 | M | **起算点改了**：从**装柜完成时间**起算，**与 ETD 解绑**。✅ `LOADING_TIME` 已有；**进港时间要加列** |
| 1.11 | 预警：装完缺柜号/铅封 | 🔧 | S | ✅ `LOADING_FLAG` + `CABINET_NO` + `CUSTOMS_SEAL_NO` 全有，纯判定。**未装完缺号不预警** |
| 1.12 | ~~预警：开船未海放/未码放~~ | ⛔ | 0 | **已合并进 1.9，不再独立**。旧代码若有「已过 ETD」分支要删 |
| 1.13 | 点预警胶囊 → 底栏自动切「全部周」 | 🆕 | S | |
| 1.14 | 筛选叠加规则 | 🆕 | S | 装完 + 提单状态可叠；**预警筛选与提单流转状态不能叠** |
| 1.15 | **业务标记列**（取消IT/海放/码放/查验/改单，合并一列） | 🆕 | M | 明确**不做**独立海放列、码放列、清关行列 |
| 1.16 | **放行标记筛选（8 值组合下拉）** | 🆕 | M | 已海放已码放/已海放未码放/未海放已码放/未海放未码放/已海放/未海放/已码放/未码放。**不能用两个 bool 接** |
| 1.17 | 行底色规则 | 🆕 | S | 取消IT 紫 · 改单中 蓝 · 未装完 橙 · 装完 绿 |
| 1.18 | 柜号与铅封合并一列同行显示 | 🔧 | S | ✅ 两列都在，纯前端合并 |
| 1.19 | 分页器 50/500，默认 500 | 🔧 | S | PRD 明确的测点 |
| 1.20 | 工具栏：取消IT标记 | 🆕 | S | **要加列**。勾「有IT」才可点，已取消不可改回 |
| 1.21 | 列表导出 | ♻️ | S | 走现有 `tx-export` 异步导出 |

**明确不做的工具栏按钮**（⛔ 不要算工时）：提单公告 · AMS/ISF · 查验标记 · 查验挂票 · 改单标记 · 进港代码 · 修改装车提单 · 作废提单 · 船单 · 截单 · 换单撤销 · 查验换船弹窗（并入换柜换船）

**明确不做的页签**：美装完 · 有IT/已取消IT/改单中/一水超限 独立页签 · 船靠24h未放行 · 甩柜提醒

---

### 2 · 提单创建（单票 / 批量 / AI 识别）

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 2.1 | 单票创建表单 | 🔧 | M | 提单号**可空**；海运费 + 国内外免用箱**必填** |
| 2.2 | Excel 式批量创建网格 | 🔧 | L | 现网已有 `/Scan/BillAddBatch` 路径 |
| 2.3 | 修改模式复用同页 | 🔧 | S | `?mode=single\|edit` |
| 2.4 | **AI 识别订舱确认书 / 提单副本** | 🆕 | XL | 三种模式：订舱确认（推荐）/ 提单副本 / 混合 |
| 2.5 | AI 字段映射（12 组） | 🆕 | M | B/L No→提单号、Container/Seal、Size/Type、POL/POD、Carrier/Vessel/Voyage、ETD（只留月-日）、Cut-off、Loading、Ocean Freight、Free time（分两格）、Transhipment、Remark |
| 2.6 | 置信度分档 + 标色 | 🆕 | S | ≥90 高 / 75–89 中 / <75 红。**置信度是行级不是字段级** |
| 2.7 | AI 写入方式（追加新行 / 覆盖空表） | 🆕 | S | |
| 2.8 | 联动①：船公司 → 提单号前缀（替换非叠加） | ⚠️ | S | **依赖提单号规则字典，那块是二期**（⚠️ P3）。三种解法见确认清单 |
| 2.9 | 联动②：船公司+POL+POD+柜型 → 海运费 + 免箱天数 | 🆕 | M | **命中维度多的优先，最长匹配** |
| 2.10 | 联动③：人工改过就 lock，后续不覆盖 | 🆕 | S | |
| 2.11 | 基础带回格视觉标记 | 🆕 | S | `.from-base` 浅绿语义 |
| 2.12 | 点价透传接收 | ⛔ | 0 | `ess_book_from_rate` 来自运价页，**运价管理本期不做**（⚠️ P2）→ 这条入口作废 |

---

### 3 · 提单分配（派拖车）

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 3.1 | 分配抽屉（6 个分区） | 🆕 | L | 顶栏/地址·仓库装柜/地址·工厂装柜/车辆信息/拖车公司/企微文案 |
| 3.2 | 装柜方式切换（仓库装柜 / 工厂装柜）字段集不同 | 🆕 | M | 仓库装柜有 装柜地址1/2 + 预计装柜时间1/2；工厂装柜是 工厂地址1/2 + 单个预计装柜时间 |
| 3.3 | 拖车公司查询价格 / 展开全部 | 🔧 | M | ✅ **数据源已存在**：`S_PRICE_TRUCK_HEAD` + `_UNIT_PRICE` + `_WEIGHT`。原型「数据为空」只是没接上，不是没数据 |
| 3.4 | 确定写回：拖车公司/地址/时间/客户/车辆 | 🆕 | M | |
| 3.5 | **装柜时间写回提单列表** | 🆕 | S | 这是 1.3 周分组的**数据来源**，必须先有 |
| 3.6 | 状态流转 待分配 → 待装柜 | 🔧 | S | |
| 3.7 | 生成派单文案 + 一键复制 | 🆕 | M | 绿色气泡预览，**可改** |
| 3.8 | 选择企微群 | ⚠️ | S | 只是选，**不推送**（推群二期） |
| 3.9 | 智能推荐拖车 | ⚠️ | L | ⚠️ W6 规则维度与推荐策略未定。**如果本期只做手工指派，这条砍掉** |

---

### 4 · 换柜换船 / 换柜不换船 / 查验换船

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 4.1 | **三场景选择入口**（必选，不能默认进某一个） | 🆕 | S | **本轮从「二选一」变「三选一」**，新增「换柜不换船」 |
| 4.2 | 换柜换船：改提单号 + 船名航次 + 柜号 + 铅封 + 预计装柜/送达时间 | 🆕 | M | |
| 4.3 | 换柜不换船：只改柜号 + 铅封 | 🆕 | S | **本轮新增** |
| 4.4 | 查验换船：改提单号 + 船名航次，**柜号只读** | 🆕 | M | 替代现网「换单撤销」 |
| 4.5 | 校验：新提单号/船名航次不能空；**新号不得与原号相同** | 🆕 | S | |
| 4.6 | 轨迹按柜号**延续到新提单，不拆分** | 🆕 | M | 只在查验换船场景 |
| 4.7 | 站内消息（默认勾选，发业务员+客服） | ♻️ | S | 按提单关联带出，不另选通讯录；可取消只保存 |
| 4.8 | **同步报关管理改单提醒** | 🆕 | M | 类型=改单，来源=船务。接口格式本期不定，但**必须预留落点** |
| 4.9 | 换单后旧号新号都能查到同一链路 | 🆕 | M | 影响 5.x 操作日志的查询逻辑 |

**明确不做**：通知关务/资料员（重发资料走改配 GW014）· 发明细邮件 · 自动打改单标记

---

### 5 · 操作日志

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 5.1 | 日志挂**提单号 + 柜号**双维度 | 🔧 | S | `S_SYS_OPERATION_LOG` 已有，单主键 `PRIMARY_NO`。写两行（一行提单号一行柜号）或加列即可，从 M 降到 **S** |
| 5.2 | **只在提交成功时写** | 🆕 | S | |
| 5.3 | 正文模板 `{类型}: [字段: 旧→新]` | 🆕 | M | **修改只记 diff**，不是全量快照 |
| 5.4 | 抽屉展示（时间/操作人/类型/内容）+ 按类型筛 + 新的在上 | 🆕 | M | |
| 5.5 | 记录 11 类操作 | 🆕 | M | 创建、修改、换绑（换柜换船/查验换船/绑定进口商）、提单分配、查验、加费用（含改删）、改单、取消IT、进港代码、提单删除 |
| 5.6 | 系统回写也记（操作人=系统） | 🆕 | S | 装完、海放/码放、IT有/无 |
| 5.7 | 删除后按旧提单号/柜号仍可查 | 🆕 | M | 日志不能跟着提单硬删 |

**明确不记**：查询 · 导出 · 舱位播报 · 复制到剪贴板 · AMS/ISF

---

### 6 · 费用登记

> **注意**：这块上一版被判为「本期不做」，**本轮反转成本期核心**，提单管理分册 PRD 有 13 行验收细则。
>
> 🔧 **代码实扫结论：后端已有约 70%**。`BalancePayEstimateServiceImpl.savePayEstimate` 已支持 `case "提单"` / `case "运单"` / `case "多运单"`，费用类型字典、批量导入映射、锁账校验、操作日志枚举全都现成。详见 4.9。
> ⚠️ **但真成本集中在三个「逆改造」点**，不是新增 —— 见 6.4 / 6.8。

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 6.1 | 费用登记弹窗（后端 API） | ✅ **已完成** | 0 | `savePayEstimate` + `importExternalBatch` 都在 `S_BALANCE_PAY_ESTIMATE` |
| 6.2 | 10 种费用类型 | ♻️ | S | **纯配置**：插 `S_BALANCE_FEE_TYPE`，`FEE_BIG_CLASS` 正好对应国内/国外。但 10 个词 Java/SQL **一处都没有**，全是新字典项 |
| 6.3 | **三条挂载路径**（挂提单 / 挂柜 / 挂票） | 🔧 | S | ✅ **挂提单、挂票已在线**（`BILL_TYPE` 支持 6 种）。**只缺「挂柜」**：加 `case "柜"`，柜身份取 `S_BUSS_SAILING_POS_CABINET.CABINET_NO`，**不用新表不用改表** |
| 6.4 | 多柜时**按提单去重，不均摊** | ⚠️ | **L** | ⚠️ **最大的坑，是逆改造不是新增**。现状：提单类型预估**会被 `PayEstimateShare` job 按「收货实际体积」强制分摊**到运单，登记完就被摊掉。要「不均摊」必须在已跑的 job 上开旁路 |
| 6.4b | **「按提单去重」的真实语义已查清** | ✅ | 0 | 柜与提单 **1:1**（`S_BUSS_SAILING_POS_CABINET.PICK_NO` 注释：2026-08-14「一柜一提单」，含 `-N` 后缀）。主表 `PICK_NO` 是 base 主提单号 → **去重 = 按 base 主提单号去重**。**N10 可据此收口** |
| 6.5 | 加载加费强制挂提单 + 6 种加载原因 | 🔧 | S | 挂提单已支持；原因走字典。⚠️ N10 挂提单后留不留柜号仍待定 |
| 6.6 | 费用大类：**国内 / 国外** | ♻️ | S | ✅ `FEE_BIG_CLASS` 现成，PRD 粘贴表的「费用大类」直接对上 |
| 6.7 | 费用来源（手工 / 邮件 / 表格导入） | 🔧 | S | ✅ `FeeBatchImportUtil.buildFeeTypeMap/buildServiceMap/buildCurrencyMap` 可直接接 PRD 的粘贴表 |
| 6.8 | 同步对象三选一 | ⚠️ | **L** | ⚠️ **有列无逻辑**：`IS_SYNC` 全仓**无任何写入方**（只有 getter/setter + 只读 resultMap）。预估 → `S_BALANCE_PAY_THEORY`（业务成本）同步链路**要从零写**，还要想清幂等与反向删除 |
| 6.8b | 「仅本柜标记」 | 🆕 | M | 无对应列与逻辑 |
| 6.9 | 费用改 / 删 + 写日志 | 🔧 | S | ✅ `OperationTypeEnum.NEW_ESTIMATED_COST` 已埋好；`BalanceFeeLockUtil` 三档锁账校验现成 |
| 6.10 | 删除提单时的费用处理 | ⚠️ | M | ⚠️ W1 三个备选未定。✅ `PayEstimateDelete` job 已存在，改动要一起考虑 |
| 6.11 | **提单管理页的费用入口** | 🆕 | M | `ScanPickBillController` 26 个端点**无一个涉及费用**，前端入口 + 新端点都要加 |

**⚠️ 新发现的边界问题（要确认）**：`S_BALANCE_COST_DETAIL`（应收费用明细）**没有 `BILL_TYPE` 列，天生只认运单**。本期需求没提提单费用进应收，但**建议先把边界问清**，否则后续要求加就是另一笔改表成本。→ 记为 **C4**。

**本轮砍掉的**（⛔ 不算工时）：九费用节点（提柜/还柜/截单/进港/开港/落箱/查验/加载/尾端异常）· 供应商台账 · 费用导入页签 · 费用草稿 · **水单全套**（上传/发送/仅上传/草稿三策略 + 水单列）

---

### 7 · 待配仓列表

> **职责本轮被切窄** —— 智能配舱逻辑全部搬到「终配舱」独立模块。待配仓**只剩一个落库动作**。

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 7.1 | 列表分页查询（43 列） | 🔧 | M | |
| 7.2 | 勾选列 + 序号**冻结** | 🆕 | S | |
| 7.3 | **勾选前 N 行批量落预配舱号** | 🆕 | M | **这是本模块唯一的落库动作**（写 `preNo`） |
| 7.4 | 批量改预配舱单号 | 🆕 | M | 完整改号链路 |
| 7.5 | 分组勾选联动 | ⚠️ | M | ⚠️ N1 **已确认有 bug**：组头与组内行状态不联动。正确口径待定 |
| 7.6 | **合并报关拆柜提醒** | ⚠️ | M | ⚠️ N13 「无故拆柜」判定标准未定。**这是本模块唯一新增的业务约束** |
| 7.7 | 容量校验 | ⚠️ | M | ⚠️ N2 **原型完全没有校验代码**，做不做未定。但 ✅ **柜型容量数据已有**（`CABINET_TYPE` / `CONTAINER_VOLUME`），要做的话不缺数据，从 L 降到 **M** |
| 7.8 | 分页器 50/500，默认 500 | 🔧 | S | |

**⛔ 不在本模块**（搬到终配舱了）：五档自动配舱 · 反倾销约束 · 服装货品名匹配 · 终配舱号生成

---

### 8 · 终配舱（本轮新独立模块）

> 从「待配仓的一个动作」升格为独立模块：独立分册 PRD 13.2 KB + 独立原型 41 KB。
> **好消息**：五档规则、反倾销约束、舱号格式**逐字未变**，上一轮的口径可直接沿用。
>
> 🔧 **表已存在**：`S_SCAN_FINAL_STOWAGE`（38 字段），装填算法的输入（票数/重量/体积/销售产品名/柜型容积）**全都齐了**。
> ⚠️ **但这是全项目最大的改造风险**：`ScanFinalStowageServiceImpl` **244 KB**、`ScanFinalStowageMapper.xml` **61 KB**，且 `tx-pda` 侧还有 **67 KB** 同表逻辑。**改这张表必须回归 PDA 装柜流程**（部署 tx-oms + tx-pda）。
> ⚠️ **命名冲突**：代码里的 `FinalStowage` 是**现有装柜/配载单**，跟 PRD 新提的「终配舱」撞名但不是一回事，沟通时务必说清。

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 8.1 | 终配舱列表页 | 🔧 | M | 表 + Controller + Mapper 都在，扩列即可 |
| 8.2 | **五档装填算法** | 🆕 | XL | 合并报关 → 服装货 → 托盘货 → 报关件 → 买单（仅补位）。**一票只入一档**，多标签取最高档。✅ 输入字段（票数/重量/体积/柜型容积）已齐 |
| 8.3 | 按渠道切分 + **跨渠道混柜硬拦** | 🆕 | M | **本轮从软提示改成硬拦，不给强制通道** |
| 8.4 | **服装货品名包含匹配** | ⚠️ | L | ⚠️ **P4 词表原文找不到**。规范化顺序：去首尾空格→去中间空格→转小写→全角转半角。**不做**全等/编辑距离/拼音 |
| 8.5 | 服装货取数=主品名 + 各明细品名，任一命中即整票算 | 🆕 | M | |
| 8.6 | 排除词逻辑（命中即不判服装） | ⚠️ | S | 同 P4。例：`服装衣架` 不入服装档 |
| 8.7 | **反倾销硬约束** | ⚠️ | L | ⚠️ W5 反倾销数据结构未定（源表暂空）。属性标记与货类档**并行**，不占单独档 |
| 8.8 | 反倾销集中装柜 + 溢出开下一柜只收反倾销 | 🆕 | M | 不得掺进已排好的非反倾销柜 |
| 8.9 | 反倾销柜有空位：默认可填 / 人工确认后锁柜空位不补 | 🆕 | M | |
| 8.10 | 柜容积与限重按柜型配置 | 🔧 | M | ✅ `CABINET_TYPE` + `CONTAINER_VOLUME` 已在 `S_BUSS_SAILING_POSITION`，7.7 的容量校验也用这个 |
| 8.11 | **终配舱号生成** `{YYMMDD}-{销售产品名称原文}-{序号}` | 🔧 | L | ✅ `FINAL_STOWAGE_NO` + `PRODUCT_NAME` 已有，**改生成规则**。日期用**仓库当地日历**；产品名用**主数据原文不改写**；序号同天同产品递增**不补零**、**隔天重置**；连接符英文半角。⚠️ **存量单号是新旧共存还是只对新单生效，要定** |
| 8.12 | 禁止出号校验 | 🆕 | M | 销售产品为空 或 一柜混多个销售产品 → **不允许出号** |
| 8.13 | 号一经生成不因改品名/改货而变；作废不复用 | 🆕 | M | |
| 8.14 | 手改舱号（留痕 + 不得与已占用号重复） | 🆕 | M | |
| 8.15 | 保存时拦截：反倾销被拆到 ≥2 非连续专柜 / 合并报关被无故分柜 | 🆕 | M | **软提示，可强制保存留痕**（注意与 8.3 的硬拦区分） |

---

### 9 · 进口商管理

> ✅ **代码实扫结论：这个模块已经基本做完了**（`tx-oms/.../importer/` 整包，`ImporterServiceImpl` 22 KB）。详见 4.4。**工时从 12~20 人日下调到 3~5 人日。**

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 9.1 | 进口商列表 + 增删改查 | ✅ **已完成** | 0 | `S_IMPORTER` + `ImporterInfoController` |
| 9.2 | **Bond 台账** | ✅ **已完成** | 0 | `BOND_TYPE` / `BOND_NO` / `BOND_INSURANCE_CODE` / `BOND_BUY_TIME` / `BOND_EXPIRE_TIME` 都在 |
| 9.3 | Bond 次数上限 **250 次**告警 | 🔧 | S | 表上无「已用次数」列，需从绑柜表 count 或加列 |
| 9.4 | Bond **临期**判定 = 失效日 30 天内 | 🔧 | S | 有 `BOND_EXPIRE_TIME`，加判定即可 |
| 9.5 | 保险代码维护 | ✅ **已完成** | 0 | `BOND_INSURANCE_CODE` |
| 9.6 | **一水绑柜上限 ≤5** | ⚠️ | M | ⚠️ **发现真冲突**：现有 `IN_TRANSIT_OVER_FIVE_CNT` 是「在途超 **5 天**」，PRD 要的是「同 (进口商,船,预计装柜日) 超 **5 柜**」。**两回事，必须先定** |
| 9.7 | **查验率统计（≥10% 告警）** | ✅ **已完成** | S | ⚠️ **N5 撤回**。`ImporterUsageSummaryServiceImpl` 已用时间戳正确实现，`ABNORMAL_RATE=10`。只剩前端展示 |
| 9.8 | 进口商筛选/导出 | ♻️ | S | |
| 9.9 | **清关状态第 5 态「资料准备」** | ⚠️ | S | 代码有 5 态，**PRD 只写 4 态漏了这个**。要问产品 |

---

### 10 · 清关行港口配置

> 🔧 **对接层已有整套**（`S_CUSTOMS_BROKER_CONFIG` + Ability + Field + PushRecord + CallbackRecord），缺的是两张业务关系表。详见 4.5。

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 10.1 | 进口商 × 清关行 授权关系维护 | 🆕 | M | **新建关系表**。`S_IMPORTER` 上确实没有清关行字段 |
| 10.2 | 清关行可接港口配置 | 🆕 | M | **新建关系表**。清关行主表可复用 |
| 10.3 | 授权撤销 | ⚠️ | M | ⚠️ N4 **撤销后已绑的柜不回滚**。是否补回滚/提醒未定 |
| 10.4 | **不接港口的比对校验** | ⚠️ | M | ⚠️ N3 **原型从不比对**。这条到底做不做未定 |

---

### 11 · 柜子编辑 · 绑定进口商

> 🔧 绑定事实表 `S_IMPORTER_BIND_CONTAINER` 已存在（含 `CLEARANCE_STATUS` / `INSPECTED_FLAG` / `CURRENT_FLAG` / `BIND_TIME` / `UNBIND_TIME`）。

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 11.1 | 绑定进口商弹窗 | ✅ **已完成** | 0 | `ImporterBindContainerService` + Mapper 齐全 |
| 11.2 | **联动落清关行** | ⚠️ | M | **真新增**：绑定表上没有清关行字段。⚠️ **N9 关务侧只读消费，不校验不兜底** → 落库正确性 100% 船务侧责任 |
| 11.3 | 绑定时校验一水上限 | ⚠️ | M | 依赖 9.6 的口径冲突先定 |
| 11.4 | 绑定写操作日志 | ♻️ | S | 走 `S_SYS_OPERATION_LOG` |

---

### 12 · 客户协议价格维护

> **本轮功能大改**：全量重填被删、大客户调成本被删，换成**发布协议价表 + 生效期间互斥**。

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 12.1 | 协议价列表 + 维护 | 🆕 | L | |
| 12.2 | **发布协议价表**（新表） | 🆕 | L | 取代原「优先级」机制 |
| 12.3 | **生效期间互斥校验** | ⚠️ | L | ⚠️ **N8 有四个漏洞**：同键区间重叠、无限期与后续冲突、边界日归属、改期间时的重算。**四个都要后端补** |
| 12.4 | `overrides` 覆盖项 | ⚠️ | L | ⚠️ **N7 现在是字符串拼接键，必须拆成子表**，否则查不动也改不动 |
| 12.5 | 协议价查询 / 导出 | ♻️ | S | |
| 12.6 | ~~全量重填~~ | ⛔ | 0 | 本轮删除，原型里是死代码 |
| 12.7 | ~~大客户调成本~~ | ⛔ | 0 | 本轮删除 |

---

### 13 · 产品库 · 出货要求与收费标准

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 13.1 | 出货要求维护 | 🆕 | M | |
| 13.2 | 收费标准维护 | 🆕 | M | |
| 13.3 | 附加费项与单位 | ⚠️ | M | ⚠️ W3 附加费单位口径未定 |
| 13.4 | 「有条件出」的处理 | ⚠️ | M | ⚠️ W4 硬拦还是仅预警未定 |

---

### 14 · 销售产品 · 报价管理 · 批量调价

> ⚠️ **这个模块索引总 PRD 的页面对照表漏了**（⚠️ D1），但有独立分册 PRD + 原型，按「本期做」处理。

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 14.1 | 报价列表 | 🔧 | M | 看现有 `offer` 域能复用多少 |
| 14.2 | 批量调价（按条件批量改） | 🆕 | L | |
| 14.3 | 调价预览 + 确认 | 🆕 | M | |
| 14.4 | 调价留痕 | 🆕 | S | |

---

### 15 · 下单 · FBA 录入 · 合规策略

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 15.1 | FBA 单号/仓库代码录入 | 🔧 | M | |
| 15.2 | 合规策略校验 | 🆕 | L | |
| 15.3 | 不合规提示/拦截 | 🆕 | M | |

---

### 16 · 舱位播报

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 16.1 | **四层级联选择**（上层不选，下层不可用） | 🆕 | M | |
| 16.2 | 船期字段 | ⚠️ | S | ⚠️ **X3 能不能手改，两处 PRD 口径打架**。原型标只读，且 ✅ 已有船期主数据 `S_BUSS_SAILING_SCHEDULE`（7 种截止时间）——**代码现状支持「只读」这一侧** |
| 16.3 | 播报文案生成 | 🆕 | M | |
| 16.4 | **一键复制到剪贴板** | 🆕 | S | 本期只做这个 |
| 16.5 | ~~推送企微群~~ | ⛔ | 0 | 二期 |

---

### 17 · 进港与落箱

| # | 功能点 | 类型 | 复杂度 | 依赖 / 备注 |
|---|---|---|---|---|
| 17.1 | **进港时间手工登记** | 🆕 | M | ⚠️ **X1 分册说本期不做飞驼自动抓取，164 KB 总 PRD 说做，冲突** |
| 17.2 | 落箱费计算（依赖进港时间） | ⚠️ | M | **风险**：进港时间纯手填，没人填 → **落箱费永远不出**。见「五、风险」F1 |
| 17.3 | ~~飞驼自动抓取进港~~ | ⛔ | 0 | 按分册 PRD 本期不做（待 X1 确认） |

---

## 二、表结构改动清单

> ⚠️ **建表前必须先拿到 4 个改口径的答复**（见「五、风险」B 组），否则字段设计要返工。

### 需要新建的表

> ✅ **代码实扫后，原先列的进口商主表 / Bond 表 / 清关行主表都已存在，不用新建**（详见 4.4 / 4.5）。真正要新建的只剩下面这些：

| 表（建议名） | 用途 | 关键字段 | 阻塞 |
|---|---|---|---|
| **进口商 × 清关行 授权关系表** | 10.1 · 11.2 | 进口商 × 清关行 × 授权状态 × 授权期 | — |
| **清关行 × 可接港口表** | 10.2 · 10.4 | 清关行 × 港口 | ⚠️ N3 |
| **客户协议价表 + 发布表** | 12.1 · 12.2 | 客户/产品/生效起/生效止/发布状态 | ⚠️ N8 |
| 协议价 overrides 子表 | 12.4 | **必须拆表**，现在是拼接字符串 | ⚠️ N7 |
| **服装货品名词表** | 8.4 · 8.6 | 包含词 + 排除词 | ⚠️ **P4 原文找不到** |
| 反倾销标记表 | 8.7 | 结构未定 | ⚠️ W5 |
| 出货要求 / 收费标准表 | 13 | — | ⚠️ W3 |

> ~~进口商主表~~ ~~Bond 表~~ ~~清关行主表~~ ~~终配舱表~~ —— **已存在，删除**。终配舱走现有 `S_SCAN_FINAL_STOWAGE` 改造。

### 需要改的现有表

| 表 | 改动 | 涉及功能点 | 说明 |
|---|---|---|---|
| ~~提单表加预计装柜时间~~ | ✅ **已有** `S_SCAN_FINAL_STOWAGE.EXPECTED_LOADING_TIME` | 1.3 · 3.5 | 周分组数据源现成 |
| ~~提单表加装柜完成时间~~ | ✅ **已有** `S_SCAN_FINAL_STOWAGE.LOADING_TIME` / `LOADING_FLAG` | 1.10 · 1.11 | 现成 |
| `S_BUSS_SAILING_POSITION` | 加 **海放标记 / 码放标记** | 1.9 · 1.15 · 1.16 | 海放 `TR00803` · 码放 `A00178`。**全库无实现** |
| `S_BUSS_SAILING_POSITION` | 加 **取消IT 标记** | 1.20 | ⚠️ **X2 船务侧两值 vs 关务侧三值**（含 `取消中`） |
| `S_BUSS_SAILING_POSITION` | 加 **改单标记** | 1.17 · 4.8 | |
| `S_BUSS_SAILING_POSITION` | 加 **进港时间** | 17.1 | 注意已有 `ENTRY_PORT_CODE` 是代码不是时间 |
| `S_BUSS_SAILING_POSITION` | 加 **预配舱号 `preNo`** | 7.3 | |
| `S_SCAN_FINAL_STOWAGE` | `FINAL_STOWAGE_NO` **生成规则改成** `{YYMMDD}-{销售产品名称}-{序号}` | 8.11 | ⚠️ **存量单号怎么办要定**（新旧格式共存 / 只对新单生效） |
| `S_SCAN_FINAL_STOWAGE_CUSTOMS` | 清关状态枚举补 | 9.7 | 代码实为 5 态：`资料准备` / `待清关` / `清关查验` / `清关放行` / `查验放行`。**PRD 只写了 4 态，漏了「资料准备」** |
| ~~加清关查验时间戳~~ | ✅ **已有** `CUSTOMS_VERIFY_START_TIME` / `CUSTOMS_VERIFY_PASS_TIME` / `CUSTOMS_PASS_TIME` | 9.7 | **N5 撤回** |
| `S_IMPORTER_USAGE_SUMMARY` | `IN_TRANSIT_OVER_FIVE_CNT` 语义冲突 | 9.6 | ⚠️ **现有=超 5 天，PRD=超 5 柜。必须先定** |
| 费用表 | 大类改 国内/国外 | 6.6 | 旧值 `国内港前`/`国外尾端` **要写兼容映射** |
| `S_SYS_OPERATION_LOG` | 柜号维度 | 5.1 | 单主键 `PRIMARY_NO`，写两行或加列即可 |

### 需要改的枚举 / 字典

| 枚举 | 改动 |
|---|---|
| 提单状态 | 「待录卸」→「**待装柜**」 |
| 放行标记筛选 | 从两个独立 bool → **8 值组合枚举**（1.16） |
| 预警类型 | 5 类，**删掉「开船未海放/未码放」**（1.12） |
| 费用类型 | 10 种（6.2）走字典 |
| 加载原因 | 6 种，含「其他」需填说明（6.5） |
| ~~运价状态~~ / ~~合约价确认状态~~ | ⛔ 本期不做，不要建 |

---

## 三、跨组 / 外部接口

| # | 对接方 | 内容 | 类型 | 复杂度 | 备注 |
|---|---|---|---|---|---|
| I1 | **关务组** | 改单提醒（类型=改单，来源=船务） | 🆕 | M | 4.8。**接口格式本期不定，先预留落点** |
| I2 | **关务组** | 进口商 × 清关行 只读消费 | 🆕 | S | ⚠️ N9 **关务不校验不兜底，无护栏** |
| I3 | 飞驼 | 进港时间自动抓取 | ⛔ | 0 | ⚠️ X1 冲突。分册说不做 |
| I4 | 企业微信 | 播报推群 / 派单推群 | ⛔ | 0 | 二期，本期只做复制 |
| I5 | 船司官网 | 海放/码放状态 | 🔧 | ? | 现有轨迹能力能否复用，见「四」 |

---

## 四、与现有代码的对照

> 实扫 `D:\project\tx-parent`。所有结论都带文件路径，可自行复核。

### 4.1 结论先说：三个模块的判定要下调

扫完代码后，原先按原型判的「全新」有三块**其实已经有了**，工时要往下压：

| 模块 | 原判 | **实际** | 依据 |
|---|---|---|---|
| **9 进口商管理** | 🆕 全新 | **🔧 大部分已实现** | `tx-oms/.../importer/` 整包 20+ 文件，`ImporterServiceImpl` 22 KB |
| **1 提单管理列表** | 🆕 全新 | **🔧 改造现有主表** | `S_BUSS_SAILING_POSITION` 已含提单号/柜号/铅封/船名航次/截单/免箱天数 |
| **10 清关行** | 🆕 全新 | **🔧 对接层已有** | `S_CUSTOMS_BROKER_CONFIG` + Ability + Field + PushRecord + CallbackRecord |

### 4.2 提单主表 = `S_BUSS_SAILING_POSITION`（舱位表）

`tx-common/tx-common-model/.../po/BussSailingPosition.java` — **50 个字段，需求要的大半已在**：

| 需求字段 | 现有列 | 状态 |
|---|---|---|
| 提单号 | `BILL_NO` | ✅ |
| 柜号 / 铅封 | `CABINET_NO` / `CUSTOMS_SEAL_NO` | ✅ 1.18 只是前端合列 |
| 船名 / 航次 | `VESSEL_NO` / `VOYAGE` | ✅ |
| ETD / ETA | `ETD` / `ETA` + `SECOND_ETD` / `SECOND_ETA` | ✅ 换船后的二程也有 |
| 截单时间 | `CY_CUTOFF` / `PORT_CUTOFF` | ✅ 1.8 临近截单可直接算 |
| 开舱 / 开港 | `OPEN_CABIN_TIME` / `OPEN_PORT_TIME` | ✅ |
| **国内外免用箱天数** | `POL_FREE_BOX_DAYS` / `POD_FREE_BOX_DAYS` | ✅ **2.1 的必填字段已存在** |
| 海运费 | `CONTRACT_OCEAN_FREIGHT` | ✅ |
| 进港代码 | `ENTRY_PORT_CODE` | ✅ |
| **换船标记** | `CHANGE_VESSEL_FLAG` | ✅ **模块 4 已有基础** |
| 中转港 / 中转船名航次 | `TRANSIT_PORT_NO` / `TRANSIT_VESSEL_VOYAGE` | ✅ 对应 W8 |
| 柜型 / 容积 | `CABINET_TYPE` / `CONTAINER_VOLUME` | ✅ **7.7 / 8.10 容量校验的基础数据在这** |
| 锁价 / 保舱 | `IS_LOCK_PRICE` / `IS_GUARANTEE_CABIN` | ✅ 运价本期不做，但字段是现成的 |

**真正缺的列**（要加）：

| 缺的 | 影响功能点 |
|---|---|
| 海放标记 / 码放标记 | 1.9 · 1.15 · 1.16 —— 全表扫无 `TR00803` / `A00178` / `RELEASE` 任何痕迹 |
| 取消IT 标记 | 1.20 —— 全表扫无 `CANCEL_IT` / `IT_FLAG` |
| 进港时间 | 17.1 —— 无 `ENTER_PORT_TIME`（只有代码 `ENTRY_PORT_CODE`，不是时间） |
| 预配舱号 `preNo` | 7.3 —— 无 |
| 改单标记 | 1.17 · 4.8 |

### 4.3 装柜单 = `S_SCAN_FINAL_STOWAGE`

`ScanFinalStowage.java`，38 字段。**注意命名冲突**：代码里的 `FinalStowage` 是**现有的装柜/配载单**，跟 PRD 新提的「终配舱模块」名字撞了，但**不是一回事**，评估和沟通时要说清楚。

| 需求 | 现有列 | 说明 |
|---|---|---|
| **预计装柜时间** | `EXPECTED_LOADING_TIME` | ✅ **1.3 周分组的数据源已存在**，不用新建 |
| **装柜完成** | `LOADING_TIME` / `LOADING_FLAG` / `LOADING_PIECE` | ✅ **1.10 起算点、1.11 装完判定可直接用** |
| 入仓扫描 | `SCAN_TIME` / `SCAN_FLAG` / `SCAN_PIECE` | ✅ |
| 票数 / 重量 / 体积 | `ORDER_COUNT` / `TOTAL_WEIGHT` / `TOTAL_VOLUME` / `TOTAL_VOLUME_WEIGHT` | ✅ **8.2 装填算法的输入齐了** |
| 销售产品名 | `PRODUCT_NAME` | ✅ **8.11 舱号生成、8.12 混产品校验的依据在** |
| 配舱单号 | `FINAL_STOWAGE_NO` | ✅ 但格式不是 PRD 的 `{YYMMDD}-{产品}-{序号}`，**要改生成规则** |

现有代码体量（改造成本的真实来源）：

| 文件 | 大小 |
|---|---|
| `tx-oms/.../scan/service/impl/ScanFinalStowageServiceImpl.java` | **244 KB** |
| `tx-oms/.../scan/mapper/ScanFinalStowageMapper.xml` | **61 KB** |
| `tx-oms/.../scan/entity/vo/FinalStowageVo.java` | 22 KB |
| `tx-oms/.../scan/controller/ScanFinalStowageController.java` | 14 KB |
| `tx-pda/.../scan/service/impl/ScanFinalStowageServiceImpl.java` | 67 KB |

> ⚠️ **这是工时里最大的不确定项**。244 KB 的 ServiceImpl 改动风险高，且 PDA 侧还有 67 KB 依赖同一批表。**动 `S_SCAN_FINAL_STOWAGE` 必须回归 PDA 装柜流程。**

### 4.4 进口商 —— 已实现，且比 PRD 走得更远

`tx-oms/src/main/java/com/yykj/oms/importer/` 完整一包：Controller 2 个 · Service 6 个 · Mapper 5 个 · Job 1 个 · Listener/Publisher/AsyncExecutor 各 1 个。

| 表 | 用途 | 对应功能点 |
|---|---|---|
| `S_IMPORTER` | 主数据 + **Bond 全套**（`BOND_TYPE` / `BOND_NO` / `BOND_INSURANCE_CODE` / `BOND_BUY_TIME` / `BOND_EXPIRE_TIME`）+ `POA_AUTH` + `CREDIT_LIMIT_USD` | **9.1 / 9.2 / 9.5 已完成** |
| `S_IMPORTER_BIND_CONTAINER` | 绑柜事实，含 `CLEARANCE_STATUS` / `INSPECTED_FLAG` / `CURRENT_FLAG` / `BIND_TIME` / `UNBIND_TIME` | **11.1 已完成** |
| `S_IMPORTER_USAGE_SUMMARY` | 在途数 + `IN_TRANSIT_OVER_FIVE_CNT` + **`CHECK_RATE_7D` / `_MONTH` / `_ALL` + `CHECK_RATE_ABNORMAL`** | **9.7 已完成** |
| `S_IMPORTER_CHECK_REMARK` | 查验备注多条历史 | ✅ PRD 没提，已有 |
| `S_IMPORTER_USAGE_REFRESH_TASK` | 异步刷新任务队列 + `ImporterUsageRefreshSchedule` | ✅ PRD 没提，已有 |

#### ⚠️ 重要更正：N5「查验率算不出来」是**误判**，撤回

我在 `待产品确认清单.md` 写 N5 时只看了原型。**代码里早就按时间戳实现了，而且实现是对的。**

`ImporterUsageSummaryServiceImpl.java:getCheckRateStatisticTime()`：

| PRD/原型的问题 | 代码实际做法 |
|---|---|
| 三态互斥算不出分母 | `isCheckRateBase()` = `清关放行` ∪ `查验放行` ∪ `清关查验` |
| 分子取不到 | `isInspectedStatus()` = `清关查验` ∪ `查验放行` |
| 没有时间戳 | 用 `CUSTOMS_VERIFY_START_TIME` / `CUSTOMS_VERIFY_PASS_TIME` / `CUSTOMS_PASS_TIME`（在 `S_SCAN_FINAL_STOWAGE_CUSTOMS`），取不到才兜底 `BIND_TIME` |
| ≥10% 阈值 | `ABNORMAL_RATE = BigDecimal.TEN`，7天/月/累计**任一**超标即标异常 |

**→ N5 不用问产品了，9.7 工时从 XL 降为 S（只需前端展示）。**
**→ 附带发现**：代码有第 5 个清关状态 `资料准备`，PRD 四态里没有。这个**要问**。

#### ⚠️ 但发现一个真冲突：`IN_TRANSIT_OVER_FIVE_CNT` 的「5」含义不同

```java
if (source.getBindTime() != null && source.getBindTime().before(fiveDaysAgo)) {
    inTransitOverFiveCnt++;
}
```

**现有代码：在途未清关 且 绑柜时间超过 5 天 → 计数。这是「超 5 天」。**
**PRD 9.6：同 (进口商, 船, 预计装柜日) 绑柜数 ≤ 5 柜。这是「超 5 柜」。**

**两个完全不同的东西，同一个字段名。** 这条必须确认清楚是改口径还是并存两个指标，否则要么算错、要么白做。

### 4.5 清关行 —— 对接层已有，但缺两张关系表

已有（`tx-common-model/.../po/`）：

| 表 | 内容 |
|---|---|
| `S_CUSTOMS_BROKER_CONFIG` | 清关行主配置 + `DOCKING_STATUS` / `WEBHOOK_URL` / `API_PROVIDER` / 连通性测试 |
| `S_CUSTOMS_BROKER_CONFIG_ABILITY` | 清关行能力项（`ABILITY_CODE`） |
| `S_CUSTOMS_BROKER_CONFIG_FIELD` | 清关行动态字段（含 `ENCRYPT_FLAG`） |
| `S_CUSTOMS_BROKER_PUSH_RECORD` | 推送记录，关联 `FINAL_STOWAGE_NO` / `BILL_NO` / `PICK_NO` |
| `S_CUST_BROKER_CALLBACK_RECORD` | 回调记录 + 验签 + 匹配状态 |

**缺的**（这两张确实要新建）：
- **进口商 × 清关行 授权关系表** → 10.1 · 11.2
- **清关行 × 可接港口表** → 10.2 · 10.4

> 注意：`S_IMPORTER` 上**没有**清关行字段，`S_IMPORTER_BIND_CONTAINER` 上也没有。11.2「联动落清关行」是真新增。

### 4.6 可直接复用的通用能力

| 能力 | 现有实现 | 用在 |
|---|---|---|
| **操作日志** | `S_SYS_OPERATION_LOG`（`PRIMARY_NO` / `OPERATION_TYPE` / `OPERATION_RECORD` / `LOG_TYPE` / `TYPE`） | 模块 5。**单主键结构**，5.1 双维度需写两行或加列 → 从 M 降为 **S** |
| **站内消息** | `S_BASE_MESSAGE` + `S_BASE_MESSAGE_RECEIVE`（含 `MESSAGE_TYPE` / `PUSH_STATUS` / `MESSAGE_PRIORITY`） | 4.7 直接复用 |
| **轨迹日志** | `S_SCAN_PICK_TRACK_LOG` | 4.6 轨迹按柜号延续 |
| **拖车报价** | `S_PRICE_TRUCK_HEAD` + `_UNIT_PRICE` + `_WEIGHT` | **3.3 有数据源了**，原型「数据为空」只是没接 |
| 字典 | `S_SYS_DICT` | 6.2 费用类型 · 6.5 加载原因 |
| 异步导出 | `tx-export` 独立服务 | 1.21 · 9.8 |
| 定时任务 | `tx-job` Quartz + `ImporterUsageRefreshSchedule` 现成范式 | 9.7 刷新 |
| 数据权限 | `@DataScope` AOP（见 CLAUDE.md） | **所有新列表页都要评估接入** |
| 船期主数据 | `S_BUSS_SAILING_SCHEDULE`（含 `SI_CUTOFF` / `VGM_CUTOFF` / `BOOKING_CUTOFF` 等 7 种截止时间） | 16.2 播报船期只读的来源 → **支持「船期不该手改」这一侧** |

### 4.7 确认「不存在」的（真全新）

以下关键词**全库扫过，无任何实现**：

| 需求 | 扫描结果 |
|---|---|
| 海放 / 码放 | 无 `TR00803` / `A00178` / `SEA_RELEASE` / `WHARF_RELEASE` / `RELEASE_FLAG` |
| 取消IT | 无 `CANCEL_IT` / `IT_FLAG` |
| 进港时间 | 无 `ENTER_PORT_TIME` / `IN_PORT_TIME` |
| 预配舱号 | 无 `PRE_CABIN_NO` / `PRE_STOWAGE_NO` |
| **客户协议价** | 无 Agreement / Protocol 相关表 → **模块 12 全新，工时不下调** |
| 服装货词表 | 无 |
| 反倾销 | 无 |
| AI 识别 | 无 OCR / 识别服务接入 → **模块 2 全新，工时不下调** |

### 4.8 影响部署的模块（改完要点名发版）

| 改动 | 需要部署 |
|---|---|
| 提单列表 / 分配 / 换柜换船 / 费用 / 日志 / 进口商 / 清关行 | **tx-oms** |
| 动 `S_SCAN_FINAL_STOWAGE` 相关表或 VO | **tx-oms + tx-pda**（PDA 侧 67 KB 同表逻辑，必须回归） |
| 报关推送 / 回调 | **tx-oms + tx-open-api**（`tx-open-api` 也有 `ScanFinalStowageCustomsMapper`） |
| 列表导出 | **tx-export** |
| 进口商用量刷新 | **tx-oms**（Schedule 在 oms 内，不在 tx-job） |
| 字典新增 | **tx-system** |

---

## 五、风险与前置依赖

### A 组 · 阻塞开工（不答复就开不了工）

| 编号 | 问题 | 影响 |
|---|---|---|
| **P1** | 三份总 PRD 谁权威 | 全局验收基准 |
| **P2** | 运价管理确认整块不做 | 影响 2.12 点价入口、报价相关工时 |
| **P3** | 提单号规则二期 → AI 联动 2.8 怎么办 | 卡住 AI 识别的一个联动 |
| **P4** | **服装货词表原文在哪** | **卡住终配舱 8.4/8.6，五档算法跑不起来** |
| **X1** | 飞驼进港做不做 | 卡住 17.x，且连锁影响落箱费 |

### B 组 · 影响建表（开工前必须定，否则字段返工）

| 编号 | 问题 | 会返工的表 |
|---|---|---|
| ~~N5~~ | ~~查验率必须落时间戳~~ | **✅ 已撤回 —— 代码早已正确实现，见 4.4** |
| **新 · C1** | **`IN_TRANSIT_OVER_FIVE_CNT` 的「5」是 5 天还是 5 柜** | `S_IMPORTER_USAGE_SUMMARY`。现有代码=5 天，PRD=5 柜，**同名不同义** |
| **新 · C2** | **清关状态第 5 态「资料准备」PRD 漏了** | `S_SCAN_FINAL_STOWAGE_CUSTOMS` 枚举 |
| **新 · C3** | **存量 `FINAL_STOWAGE_NO` 怎么办**（新旧格式共存 / 只对新单生效） | `S_SCAN_FINAL_STOWAGE` |
| **N8** | 期间互斥四个漏洞 | 协议价发布表 |
| **N7** | overrides 拆子表 | 协议价 overrides |
| **X2** | 取消IT 两值 vs 三值 | `S_BUSS_SAILING_POSITION` 新增枚举 |
| **N10** | 加载加费挂提单后柜号留不留 | 费用表 |
| **W5** | 反倾销数据结构 | 反倾销表 |

### C 组 · 业务连锁风险

| 编号 | 风险 | 说明 |
|---|---|---|
| **F0** | **`S_SCAN_FINAL_STOWAGE` 是全项目最危险的改动点** | `tx-oms` 244 KB ServiceImpl + `tx-pda` 67 KB 共用同表。改动**必须双端回归、双端部署**。排期紧时建议评估**新建表旁路**方案 |
| **F1** | **落箱费永远不出** | 17.2 依赖进港时间，而进港时间**纯手工填**且飞驼自动抓取本期不做。没人填 → 费用一条都不产生 |
| **F2** | 清关行落库错了没人拦 | I2 / 11.2。关务侧只读消费，**不校验不兜底**，错了直接影响清关 |
| **F3** | 8.3 硬拦 vs 8.15 软提示 | 同一个模块两种拦截强度，实现时容易搞混 |
| **F4** | 6.6 费用大类改名 | 旧数据 `国内港前`/`国外尾端` 必须写兼容映射，否则历史费用查不出来 |
| **F5** | 4.9 换单后新旧号 | 日志、轨迹、费用三处都要能按旧号查到 |
| **F6** | **命名撞车** | 代码 `FinalStowage` = 现有装柜单，PRD「终配舱」= 新概念。**沟通和验收时必须区分**，否则会出现「说的是同一个词做的是两件事」 |

---

## 六、明确不做（工时清零，防止误算）

| 整块移出 | 上一版是否算过工时 |
|---|---|
| 运价管理（我司与代理订舱） | 算过，**要扣** |
| 合约价确认开关 | 算过，**要扣** |
| 数据字典 · 提单号规则 | 算过，**要扣** |
| 飞驼进港自动抓取 | 算过，**要扣**（待 X1） |
| 舱位播报推企微群 | 算过，**要扣** |
| 九费用节点 / 供应商台账 / 费用导入 / 费用草稿 / **水单全套** | 算过，**要扣** |
| 协议价全量重填 + 大客户调成本 | 算过，**要扣** |
| 运单管理 · 签入代办角标 | 算过，**要扣**（待 X5/X6） |

**零散不做项**：提单公告 · AMS/ISF · 查验标记 · 查验挂票 · 改单标记按钮 · 进港代码按钮 · 修改装车提单 · 作废提单 · 船单 · 截单 · 换单撤销 · 美装完页签 · 船靠24h未放行 · 甩柜提醒 · 通知关务资料员 · 发明细邮件

---

## 七、工时汇总（初估）

> **已按第四节的代码实扫结果修正过**。修正前是 184~299 人日，扫完代码后降到 **166~253**，主要省在进口商（-12）和柜子绑定（-3）。

| 模块 | 功能点数 | 已完成 | 阻塞 | 初估人日（后端） | 相比扫码前 |
|---|---|---|---|---|---|
| 1 提单管理列表 | 21 | 0 | 0 | 14 ~ 20 | ↓ 主表与装柜时间已有 |
| 2 提单创建 · AI 识别 | 12 | 0 | 1 | 20 ~ 28 | — 全新 |
| 3 提单分配 | 9 | 0 | 2 | 9 ~ 14 | ↓ 拖车报价表已有 |
| 4 换柜换船 | 9 | 0 | 0 | 8 ~ 12 | ↓ `CHANGE_VESSEL_FLAG` 已有 |
| 5 操作日志 | 7 | 0 | 0 | 6 ~ 9 | ↓ 通用日志表可复用 |
| 6 费用登记 | 10 | 0 | 4 | 12 ~ 18 | — |
| 7 待配仓 | 8 | 0 | 3 | 7 ~ 12 | ↓ 柜型容量已有 |
| 8 **终配舱** | 15 | 0 | 4 | **25 ~ 35** | 表已有但 **244 KB 改造风险**抵消 |
| 9 进口商管理 | 9 | **4** | 2 | **3 ~ 5** | **↓↓ 大部分已实现** |
| 10 清关行港口配置 | 4 | 0 | 2 | 5 ~ 8 | — 缺两张关系表 |
| 11 柜子绑定进口商 | 4 | **1** | 2 | 3 ~ 5 | ↓ 绑定表已有 |
| 12 客户协议价 | 7 | 0 | 2 | 15 ~ 22 | — **确认全新** |
| 13 产品库出货要求 | 4 | 0 | 2 | 6 ~ 10 | — |
| 14 报价 · 批量调价 | 4 | 0 | 0 | 8 ~ 12 | — |
| 15 FBA 录入合规 | 3 | 0 | 0 | 6 ~ 10 | — |
| 16 舱位播报 | 4 | 0 | 1 | 4 ~ 7 | — |
| 17 进港与落箱 | 2 | 0 | 1 | 3 ~ 5 | — |
| — 跨组接口 | 2 | 0 | 0 | 3 ~ 6 | — |
| — 表结构与枚举改造 | — | — | — | 6 ~ 10 | — |
| — **PDA 装柜回归测试** | — | — | — | **3 ~ 5** | **新增，别漏** |
| **合计** | **134** | **5** | **25** | **166 ~ 253** | |

### 读这张表要注意

1. **25 个功能点带 ⚠️**，其中 **5 个是 A 组阻塞**（P1~P4 + X1）。**这 5 条不答复，终配舱和 AI 识别两个最贵模块开不了工。**
2. 上下限差 87 人日，差距主要来自三处：
   - **8 终配舱** —— 244 KB ServiceImpl 的改造深度，取决于能不能旁路新建而不动老逻辑
   - **2 AI 识别** —— 取决于识别服务选型（自研 / 三方 OCR / 大模型）
   - **12 协议价** —— 取决于 N8 四个期间互斥漏洞要补多少
3. **`S_SCAN_FINAL_STOWAGE` 是全项目最危险的改动点**。`tx-oms` 244 KB + `tx-pda` 67 KB 共用，**改动必须双端回归、双端部署**。如果排期紧，建议评估**新建终配舱表旁路**而不是改老表。
4. **前端工时未计**。原型都画了，但 1（43 列 + 周页签 + 预警条）、8（终配舱）、12（协议价）三个页面偏重。
5. 汇总里**没有**任何「明确不做」的内容。如果上一版排期含运价管理、水单、九费用节点，**要先扣掉再对比**。
6. **进口商模块省下来的 12 人日不要直接挪走** —— 9.6 那个「5 天 vs 5 柜」的语义冲突如果要求改口径，可能反过来吃掉一部分。

---

## 附录 · 与「待产品确认清单.md」的对应

本文档所有 ⚠️ 编号（P / X / N / W / D）与 `待产品确认清单.md` 完全一致，两份可对照使用：
- **这份**回答「要做什么、多少工时」
- **那份**回答「有什么没定、要问产品什么」

### ⚠️ 代码实扫后，那份清单要改三处

| 项 | 处理 |
|---|---|
| **N5**（查验率公式无法按快照实现） | **撤回，不用问产品**。`ImporterUsageSummaryServiceImpl` 已用 `CUSTOMS_VERIFY_START_TIME` / `CUSTOMS_VERIFY_PASS_TIME` 正确实现，阈值 10% 也在 |
| **W6**（拖车推荐数据从哪来） | **降级**。`S_PRICE_TRUCK_HEAD` 系列已有报价数据，只需确认推荐策略 |
| **新增三条要问** | **C1** 一水「5」是 5 天还是 5 柜（现有代码与 PRD 同名不同义）· **C2** 清关状态第 5 态「资料准备」PRD 漏写 · **C3** 存量终配舱号是否沿用旧格式 |

> C1 是这次扫码发现的**最值得问的一条** —— 现有字段 `IN_TRANSIT_OVER_FIVE_CNT` 已经在按「超 5 天」跑了，如果 PRD 的「≤5 柜」是新口径，要明确是**替换**还是**并存两个指标**。搞错了要么数据算错，要么白做一遍。

