# -*- coding: utf-8 -*-
"""One-off: generate Word reply to dev questions from 提成管理系统 · 问题清单.pdf"""
from pathlib import Path

from docx import Document
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.shared import Pt, Cm

OUT = Path(__file__).resolve().parent / "提成管理系统-问题清单-产品回复开发.docx"


def add_heading(doc, text, level=1):
    p = doc.add_heading(text, level=level)
    return p


def add_para(doc, text, bold=False):
    p = doc.add_paragraph()
    run = p.add_run(text)
    run.bold = bold
    run.font.size = Pt(11)
    return p


def add_block(doc, title, body_lines):
    add_para(doc, title, bold=True)
    for line in body_lines:
        p = doc.add_paragraph(line, style="List Bullet")
        for run in p.runs:
            run.font.size = Pt(11)


def main():
    doc = Document()
    for s in doc.sections:
        s.top_margin = Cm(2)
        s.bottom_margin = Cm(2)
        s.left_margin = Cm(2.2)
        s.right_margin = Cm(2.2)

    t = doc.add_paragraph()
    t.alignment = WD_ALIGN_PARAGRAPH.CENTER
    r = t.add_run("《提成管理系统 · 问题清单》\n产品侧回复（致开发）")
    r.bold = True
    r.font.size = Pt(16)

    meta = doc.add_paragraph()
    meta.alignment = WD_ALIGN_PARAGRAPH.CENTER
    m = meta.add_run(
        "依据文档：提成管理系统 · 问题清单（范围：规则包与规则 / 提成试算 / 月底归档）\n"
        "回复日期：2026-05-11 · 用途：统一口径便于开发落地（与原型迭代对齐；若与业务最终签字版冲突，以签字版为准）"
    )
    m.font.size = Pt(10)

    doc.add_paragraph()

    intro = (
        "下表逐条回应开发提出的 Q1～Q16。标注「建议」处为产品侧推荐默认实现，"
        "若业务在评审会另有裁定，以评审结论更新本文档即可。"
    )
    add_para(doc, intro)

    items = [
        (
            "Q1【阻塞】规则包列表「生效月份」筛选语义",
            [
                "开发问题：单值「生效月份」是指区间覆盖该月，还是生效起始月等于该月？",
                "产品回复：采用「生效区间与自然月 M 有交集」口径。即筛选 2026-03 时，规则包满足 "
                "not(end < 当月首) and not(start > 当月末)（与常见区间筛选一致）。",
                "说明：与「起始月精确等于」不等价；请在筛选区 helper 文案写清「覆盖该月的规则包」。",
            ],
        ),
        (
            "Q2【阻塞】编辑规则 · 计算方式三 Tab 是否可同时落库",
            [
                "开发问题：三 Tab 均可填写时，一条规则是否严格三选一？未激活 Tab 数据如何处理？",
                "产品回复：MVP 一条规则仅一种计算方式（三选一）。服务端仅持久化「当前激活 Tab」对应结构；"
                "非激活 Tab 的填写不作为引擎输入。",
                "建议实现：保存/发布时只提交激活 Tab；切换 Tab 时可在前端提示「仅当前页签会保存」；"
                "可选在切换离开 Tab 时清空未激活数据以减少误读（非强制，以开发体验为准）。",
            ],
        ),
        (
            "Q3【阻塞】结构化匹配 vs 条件表达式关系",
            [
                "开发问题：表单与表达式可矛盾，以谁为准？",
                "产品回复：MVP 以「结构化匹配区」为权威来源；条件表达式由保存时根据结构化字段生成/覆盖，"
                "或对用户开放编辑时在保存前做一致性校验，不一致则阻断并提示。",
                "引擎命中只消费与结构化对齐后的最终表达式快照（或双写一致校验通过后方可保存）。",
            ],
        ),
        (
            "Q4【逻辑矛盾】「生效中」规则点「保存草稿」后的版本与试算",
            [
                "开发问题：原生效版本是否继续生效？试算用哪版？",
                "产品回复：保存草稿应生成/更新「草稿版本」，当前已发布且生效中的版本继续对外生效，直至「保存并发布」产生新版本。",
                "试算默认仍绑定「当前生效发布版本」；若产品提供「按草稿试算」入口，需显式选择草稿版本号（可二期）。",
            ],
        ),
        (
            "Q5【缺失定义】匹配优先级含义",
            [
                "开发问题：数值大小、比较范围、同优先级处理？",
                "产品回复：数值越大越优先；优先级用于同一规则包内、同一互斥/编排上下文下的排序（以规则包配置为准，跨包不直接比较该字段）。",
                "同优先级：MVP 建议阻断保存并提示调整；或按规则编码字典序稳定次序（二选一并写死一处即可）。",
            ],
        ),
        (
            "Q6【缺失定义】累计规则三口径",
            [
                "开发问题：累计基数、年限起点、上限含义？",
                "产品回复（建议默认，待财务签字）：① 累计加成的基数为「基本提成点」而非档位命中比例；"
                "② 年限自员工入职日期起算（满整年 +2%，不足年不计）；③「累计上限」为「最终提成比例」封顶（非年增量单独上限）。",
                "若与财务纸质方案不一致，以财务最终口径替换本段。",
            ],
        ),
        (
            "Q7【阻塞】试算批次触发入口",
            [
                "开发问题：无发起试算按钮；自动还是手动？与归档关系？",
                "产品回复：MVP 支持用户手动「发起试算 / 重新试算」（入口：提成试算表页顶主按钮区；列表页可提供快捷入口）。",
                "定时任务可作为二期；与月底归档跑批可分离——试算不依赖归档日，但「确认发放」前需满足你们定义的前置校验（如月度调整已入账）。",
            ],
        ),
        (
            "Q8【逻辑矛盾】「计算净利润」与上月毛利结转",
            [
                "开发问题：列头毛利−毛利扣与上月结转 −200 如何同时成立？",
                "产品回复：将「计算净利润」严格定义为毛利−表内毛利扣（与列头一致）；「上月毛利调整结转」为阶梯（或比例）计提基数前的独立调整项。",
                "阶梯基数 = 计算净利润 + 上月毛利调整结转（示例：31335 + (−200) = 31135）。请同步修正表头公式说明文案，避免误解。",
            ],
        ),
        (
            "Q9【逻辑矛盾】「团队池 50% 后置」",
            [
                "开发问题：原型无配置入口。",
                "产品回复：MVP 不包含「团队池 / 后置分成」能力；当前试算表该行示例为占位/错误展示，请改为已支持规则类型或标注「演示数据」。",
                "若业务坚持保留概念，请单独开二期需求（配置层级、池口径、分配顺序）。",
            ],
        ),
        (
            "Q10【缺失定义】「是否计提=否」行与勾选发放",
            [
                "开发问题：否行能否进批次？全选是否包含？是否可手改？",
                "产品回复：是否计提=否 的行不得纳入发放批次；勾选无效或置灰该行勾选；「全选当前视图」不选中否行。",
                "字段由系统根据规则/数据自动判定，MVP 不提供用户手改。",
            ],
        ),
        (
            "Q11【缺失定义】绩效系数作用点",
            [
                "开发问题：乘在哪一步？配置在哪？试算表如何展示？",
                "产品回复（建议）：绩效系数来自月度调整表，按员工+账期读取；作用在「阶梯/规则公式产出未封顶提成之后」，再参与封顶/保底（若有）。",
                "试算表增加「绩效系数」「绩效调整后应发」列便于财务核对；日志步骤与表列对齐。",
            ],
        ),
        (
            "Q12【待确认】确认发放弹窗「已确认」",
            [
                "开发问题：checkbox 还是纯文案？",
                "产品回复：必须为用户可勾选的确认项；未勾选时「生成发放批次」置灰不可点。文案可保留为法律/内控提示。",
            ],
        ),
        (
            "Q13【逻辑矛盾】归档跑批 24 日 vs PRD 23 日",
            [
                "开发问题：同一页两处不一致。",
                "产品回复：统一为「每月 24 日 00:00」触发（与页面顶部调度说明一致）；请同步修订页面底部 PRD 摘录及所有对外说明。",
            ],
        ),
        (
            "Q14【逻辑矛盾】定时调度 vs 批次冻结条件",
            [
                "开发问题：未发放是否阻断跑批？批次追加还是覆盖？",
                "产品回复（建议）：调度仍可按计划触发「归档任务」，任务内部对未满足冻结条件的账期「跳过生成/不覆盖已冻结批次」；"
                "已冻结批次不允许被后续调度静默改写，需走人工解冻/冲正流程（若业务有）。",
                "请输出明确状态机文档（待开发细化表结构）。",
            ],
        ),
        (
            "Q15【逻辑矛盾】归档批次「数据范围」示例不一致",
            [
                "开发问题：ARCH_202603 与 202602 范围口径矛盾。",
                "产品回复：示例数据错误。数据范围口径统一为「相对数据账期往前 8 个自然月的运单集合」（或你们已定稿的窗口定义），"
                "两条示例应同一算法生成；请修正原型与验收用例。",
            ],
        ),
        (
            "Q16【缺失定义】运单底表规则编码留空",
            [
                "开发问题：何时写入？试算时空值如何处理？",
                "产品回复：规则编码为「命中结果属性」，试算/计提跑批在结果明细中写入；底表第一版允许留空。",
                "试算时由引擎按映射与规则包解析命中规则，不在底表强依赖该列；归档/发放确认后可将命中规则编码回写底表（以实现为准，需防重复覆盖）。",
                "「第一版留空」= 业务初始化期允许，开发需兼容空值走引擎匹配。",
            ],
        ),
    ]

    for title, lines in items:
        doc.add_paragraph()
        add_heading(doc, title, level=2)
        for line in lines:
            p = doc.add_paragraph(line)
            p.paragraph_format.space_after = Pt(3)
            for run in p.runs:
                run.font.size = Pt(11)

    doc.add_paragraph()
    add_heading(doc, "附：优先级处理说明", level=2)
    add_para(
        doc,
        "开发在清单末尾标注需优先处理阻塞项 Q1、Q2、Q3、Q7；请按上文明确定义排期。"
        "若实现中发现仍无法落库，请单条拉会并 @ 产品补充验收标准与边界用例。",
    )

    doc.save(OUT)
    print("Wrote:", OUT)


if __name__ == "__main__":
    main()
